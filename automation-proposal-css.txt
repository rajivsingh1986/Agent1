/* ══════════════════════════════════════════════════════════
   Automation Feasibility & RPA Proposal — CSS Specification
   ══════════════════════════════════════════════════════════ */

*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
body {
    font-family: 'Segoe UI', Calibri, Arial, sans-serif;
    font-size: 13.5px; line-height: 1.55;
    background: #E4E9EF; color: #1A2535; padding: 28px 20px;
}

/* ── Page header ── */
.page-hdr {
    background: linear-gradient(135deg, #0D1F3C 0%, #1A4080 100%);
    color: white; padding: 28px 36px; border-radius: 12px;
    margin-bottom: 32px; box-shadow: 0 6px 24px rgba(0,0,0,0.22);
}
.page-hdr h1 { font-size: 23px; font-weight: 700; letter-spacing: 0.2px; }
.meta-row {
    margin-top: 12px; display: flex; gap: 28px; flex-wrap: wrap;
    font-size: 13px; opacity: 0.88;
}
.meta-row span b { font-weight: 700; }

/* ── Section card (numbered sections) ── */
.section-card {
    background: white; border-radius: 12px; margin-bottom: 32px;
    box-shadow: 0 4px 18px rgba(0,0,0,0.10); overflow: hidden;
}
.section-hdr {
    background: #0D1F3C; color: white; padding: 15px 28px;
    font-size: 16px; font-weight: 700; display: flex; align-items: center; gap: 12px;
}
.section-num {
    background: white; color: #0D1F3C; border-radius: 50%;
    width: 30px; height: 30px; font-size: 14px; font-weight: 800;
    display: flex; align-items: center; justify-content: center; flex-shrink: 0;
}
.section-body { padding: 24px 28px; }
.section-body p { margin-bottom: 14px; }
.section-body h3 { font-size: 15px; color: #0D1F3C; font-weight: 700; margin: 18px 0 10px; }
.section-body h4 { font-size: 14px; color: #1A4080; font-weight: 700; margin: 14px 0 8px; }
.section-body ul, .section-body ol { padding-left: 22px; margin-bottom: 12px; }
.section-body li { margin-bottom: 5px; }

/* ── Badge pills ── */
.pill { padding: 3px 10px; border-radius: 4px; font-size: 11px; font-weight: 700;
    color: white; letter-spacing: 0.4px; display: inline-block; }
.p-high { background: #1A7A3C; }
.p-med  { background: #D4A017; }
.p-low  { background: #A93226; }
.p-auto { background: #1A7A3C; }
.p-hyb  { background: #1F618D; }
.p-man  { background: #6C3483; }
.p-cond { background: #7D6608; }
.p-rpa  { background: #154360; }
.p-api  { background: #2C7873; }
.p-ai   { background: #8E44AD; }

/* ── Data tables ── */
.data-tbl { width: 100%; border-collapse: collapse; font-size: 12.5px; margin-bottom: 14px; }
.data-tbl th { background: #0D1F3C; color: white; padding: 9px 10px; text-align: left; font-weight: 600; font-size: 11.5px; }
.data-tbl td { border: 1px solid #AED6F1; padding: 8px 10px; vertical-align: top; }
.data-tbl tr:nth-child(even) td { background: #F4F9FF; }
.data-tbl td:first-child { font-weight: 700; color: #0D1F3C; white-space: nowrap; width: 55px; }

/* ── Metric cards ── */
.metric-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(180px, 1fr)); gap: 16px; margin: 16px 0; }
.metric-card { border-radius: 8px; padding: 18px 16px; text-align: center; }
.metric-card .num { font-size: 28px; font-weight: 800; display: block; }
.metric-card .lbl { font-size: 12px; font-weight: 600; margin-top: 4px; }
.mc-blue { background: #EBF3FC; border-left: 5px solid #0D1F3C; }
.mc-green { background: #E9F7EF; border-left: 5px solid #1A7A3C; }
.mc-amber { background: #FEF9E7; border-left: 5px solid #D4A017; }
.mc-red { background: #FDECEA; border-left: 5px solid #A93226; }

/* ── Phase cards (implementation roadmap) ── */
.phase-card { border-radius: 9px; padding: 20px 22px; margin-bottom: 18px; }
.phase-card h4 { margin-top: 0; }
.phase-card ul { padding-left: 20px; }
.phase-card li { margin-bottom: 4px; }
.ph1 { background: #E9F7EF; border-left: 6px solid #1A7A3C; }
.ph2 { background: #EBF3FC; border-left: 6px solid #1A4080; }
.ph3 { background: #F5EEF8; border-left: 6px solid #8E44AD; }

/* ── Summary cards (grid layout) ── */
.summary-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin-top: 16px; }
.s-card { border-radius: 8px; padding: 18px 20px; font-size: 13px; line-height: 1.55; }
.s-card h4 { font-size: 14px; font-weight: 700; margin-bottom: 10px; }
.s-card ul { padding-left: 18px; }
.s-card li { margin-bottom: 5px; }
.card-blue   { background: #EBF3FC; border-left: 5px solid #0D1F3C; }
.card-green  { background: #E9F7EF; border-left: 5px solid #1A7A3C; }
.card-amber  { background: #FEF9E7; border-left: 5px solid #D4A017; }
.card-red    { background: #FDECEA; border-left: 5px solid #A93226; }
.full-row    { grid-column: 1 / -1; }

/* ── Risk table ── */
.risk-tbl { width: 100%; border-collapse: collapse; font-size: 12.5px; }
.risk-tbl th { background: #0D1F3C; color: white; padding: 9px 12px; text-align: left; font-weight: 600; }
.risk-tbl td { border: 1px solid #AED6F1; padding: 9px 12px; vertical-align: top; }
.risk-tbl tr:nth-child(even) td { background: #F4F9FF; }

/* ── Footer ── */
.page-footer {
    text-align: center; font-size: 11.5px; color: #777;
    padding: 20px; margin-top: 10px;
}

@media print {
    body { background: white; padding: 0; }
    .section-card { box-shadow: none; }
    @page { size: A4 portrait; margin: 12mm; }
}

